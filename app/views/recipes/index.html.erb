<div>
  <h1>Recipes</h1>

  <%= link_to 'New Recipe', new_recipe_path, class: 'new-recipe-button' %>
  <table>
    <tbody>
      <% @recipes.each do |recipe| %>
        <tr>
          <td><%= link_to recipe.name, recipe %></td>
          <td>description: <%= recipe.description %></td>
          <td>
            <% if can?(:destroy, recipe) %>
              <%= button_to "Remove",
                            recipe,
                            method: :delete,
                            title: "Delete recipe",
                            class: "delete" %>
            <% end %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
